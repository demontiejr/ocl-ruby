context Transacao::programa()
	post fazNada: programa().obtemServicos()->select(condicao=true)->size() + 2
	
context Cliente::idade()
    post: nome = if masculino then 'jose' else 'maria' endif
    post: cartoes->forAll(valido) = true